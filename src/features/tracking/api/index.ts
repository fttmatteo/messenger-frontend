/**
 * API de Tracking
 *
 * Servicios para:
 * - GET /api/tracking/active: Mensajeros activos
 * - GET /api/tracking/messenger/{id}: Última ubicación
 * - GET /api/tracking/history/{id}: Historial de ruta
 * - WebSocket /ws/tracking: Actualizaciones en tiempo real
 */

// Los servicios de tracking se exportarán aquí cuando se implementen
