/**
 * API de Service Delivery (Entregas)
 *
 * Servicios HTTP para:
 * - GET /services: Listar entregas
 * - GET /services/{id}: Obtener por ID
 * - POST /services/create: Crear con OCR
 * - PUT /services/{id}/status: Actualizar estado
 * - GET /services/messenger/{id}: Por mensajero
 * - GET /services/status/{status}: Por estado
 */

// Los servicios de service-delivery se exportarán aquí cuando se implementen
