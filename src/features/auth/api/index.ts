/**
 * API de Autenticación
 *
 * Servicios HTTP para:
 * - Login (POST /auth/login)
 * - Refresh token (POST /auth/refresh)
 * - Logout (limpieza de tokens)
 */

// Los servicios de auth se exportarán aquí cuando se implementen
