/**
 * Módulo de Rutas
 *
 * Define la configuración de React Router:
 * - Rutas públicas (login)
 * - Rutas protegidas (dashboard, tracking)
 * - Componentes de guard (ProtectedRoute)
 */

// El router se exportará aquí cuando se implemente
